import 'package:flutter/material.dart';

class OrderIncoming extends StatelessWidget {
  const OrderIncoming({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Order Incoming")),
    );
  }
}
