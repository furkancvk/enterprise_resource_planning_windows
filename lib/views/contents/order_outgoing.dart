import 'package:flutter/material.dart';

class OutgoingOrder extends StatelessWidget {
  const OutgoingOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('giden sipariş SAYFASI'),
      ),
    );
  }
}
