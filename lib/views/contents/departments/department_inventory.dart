import 'package:flutter/material.dart';

class DepartmentInventory extends StatelessWidget {
  const DepartmentInventory({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Department Inventory")),
    );
  }
}
