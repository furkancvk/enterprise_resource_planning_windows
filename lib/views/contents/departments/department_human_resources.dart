import 'package:flutter/material.dart';

class DepartmentHumanResources extends StatelessWidget {
  const DepartmentHumanResources({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Department Human Resources")),
    );
  }
}
