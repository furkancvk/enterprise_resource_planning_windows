import 'package:flutter/material.dart';

class ProductStock extends StatelessWidget {
  const ProductStock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ürün Stok SAYFASI'),
      ),
    );
  }
}
