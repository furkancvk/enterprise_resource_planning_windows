import 'package:flutter/material.dart';

class HumanResourcesDepartment extends StatelessWidget {
  const HumanResourcesDepartment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('insan kaynkaları SAYFASI'),
      ),
    );
  }
}
