
import 'package:flutter/material.dart';

class IncomingOrder extends StatelessWidget {
  const IncomingOrder({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('gelen sipariş SAYFASI'),
      ),
    );
  }
}
