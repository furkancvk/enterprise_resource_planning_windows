import 'package:flutter/material.dart';

class MaterialStock extends StatelessWidget {
  const MaterialStock({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Hammadde Stok SAYFASI'),
      ),
    );
  }
}
