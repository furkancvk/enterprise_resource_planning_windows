import 'package:flutter/material.dart';

class DocumentBills extends StatelessWidget {
  const DocumentBills({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Document Bills")),
    );
  }
}
