import 'package:flutter/material.dart';

class DocumentReports extends StatelessWidget {
  const DocumentReports({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Document Reports")),
    );
  }
}
